# SmartBuilding v2.0 环境变量（可选）
#
# 数据库 / Redis 密码已写死在 docker-compose.yml（默认 123456），
# 无需在本文件配置。如需其他密码，直接改 compose 里对应值即可。
#
# 下面两项为可选：不设置时回退代码内置默认值；
# 如需自定义，复制本文件为 .env 后填入真实值。

# JWT 签名密钥（可选；变更会使已登录 token 失效）
# SMARTBUILDING_JWT_SECRET=

# MQTT 通道密码加密密钥（可选；不设置回退内置默认值，兼容历史已加密数据）
# 如需自定义，必须是 32 字节 Fernet key 的 urlsafe-base64 字符串
# SMARTBUILDING_FERNET_KEY=

# 数据卷宿主目录（可选，默认相对 compose 文件所在目录）
# DOCKER_VOLUME_DIRECTORY=/data/smartbuilding
